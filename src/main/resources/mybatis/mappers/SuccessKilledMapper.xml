<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baicai.mapper.SuccessKilledMapper">

    <!-- 根据联合主键seckillId&&userPhone，查询秒杀记录 -->
    <select id="queryBySeckillIdAndUserPhone" resultType="SuccessKilled">
        SELECT sk.seckill_id, sk.user_phone, sk.create_time, sk.state,
            s.seckill_id  "seckill.seckill_id",
            s.name "seckill.name",
            s.number "seckill",
            s.start_time  "seckill.start_time",
            s.end_time  "seckill.end_time",
            s.create_time "seckill.create_time"
        FROM success_killed sk
        INNER JOIN seckill s ON sk.seckill_id = s.seckill_id
        WHERE sk.seckill_id = #{seckillId} AND sk.user_phone= #{userPhone}
    </select>

    <!-- 插入秒杀记录（IGNORE：唯一字段值已存在时，忽略错误，插入语句返回0，但主键仍会自增） -->
    <insert id="insert">
        INSERT IGNORE INTO success_killed (seckill_id, user_phone, state)
        VALUES (#{seckillId}, #{userPhone}, 0)
    </insert>
</mapper>