<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.baicai.mapper.SeckillMapper">

    <!-- 根据ID查询 -->
    <select id="queryById" resultType="Seckill">
        SELECT * FROM seckill AS s WHERE s.seckill_id = #{seckillId}
    </select>

    <!-- 分页查询秒杀商品列表 -->
    <select id="queryPageList" resultType="Seckill">
        SELECT * FROM seckill AS s ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>

    <!-- 根据ID，减库存 -->
    <update id="reduceNumber">
        UPDATE seckill SET number = number - 1
        WHERE seckill_id = #{seckillId}
              AND start_time <![CDATA[<=]]> #{killTime}
              AND end_time >= #{killTime}
              AND number > 0
    </update>

</mapper>